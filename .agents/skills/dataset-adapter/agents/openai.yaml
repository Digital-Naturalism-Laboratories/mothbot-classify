interface:
  display_name: "Dataset Adapter"
  short_description: "Build and validate dataset adapters"
  default_prompt: "Use $dataset-adapter to add support for this dataset folder shape."
policy:
  allow_implicit_invocation: true
