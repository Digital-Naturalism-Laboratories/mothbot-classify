/**
 * When true, legacy Dinalab folder trees (dataset/deployment/night/patches) can still be opened.
 * Disable after one release once Mothbox Next adapter workflow is validated in production.
 */
export const legacyIngestEnabled = true
